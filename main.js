// Main initialization — guards against duplicate autocomplete init
// (autocomplete.js already handles its own DOMContentLoaded initialization)
document.addEventListener('DOMContentLoaded', function() {
    console.log('SkyWay main.js loaded successfully.');
});